% Drawing packages
\usepackage{asymptote}
\def\asydir{assets/figures/asymptote}

\usepackage{forest}

\usepackage{tikz}
\usepackage{times}
\usetikzlibrary{trees,positioning,shapes,shadows,arrows.meta}% tikz libraries for drawing trees
\usetikzlibrary{mindmap,backgrounds} % tikz libraries for drawing mindmaps

\tikzset{%
  parent/.style  = {%
      draw, rounded corners=2pt, text width=3cm,
      align=center, font=\sffamily, rectangle,
      inner sep=.1cm, fill=blue!20
    },
  root/.style   = {parent, rounded corners=2pt, thin, align=center, fill=green!30},
  child/.style = {parent, thin, rounded corners=2pt, align=center, fill=pink!60,},
  gchild/.style = {parent, thin, rounded corners=2pt, align=left, fill=green!30, align=center},
  g2child/.style = {parent, thin, align=left, fill=yellow!30, align=center},
  g3child/.style = {parent, thin, align=left, fill=red!30, align=center},
  edge from parent/.style={draw=black, edge from parent fork left}
}